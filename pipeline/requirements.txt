# SAA Org Health Tracker — Python dependencies
# Install with: pip install -r requirements.txt

requests>=2.31.0        # HTTP requests — Supabase REST API, ProPublica, website fetching
apify-client>=1.6.0     # Apify platform client (Instagram scraping)
python-dateutil>=2.8.2  # Date parsing utilities
pyairtable>=2.3.0       # Migration only — remove after migrate_to_supabase.py is run
anthropic>=0.25.0       # Claude API — org classification in discovery pipeline
beautifulsoup4>=4.12.0  # HTML parsing — extract text from org websites
